# Backend Settings
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Frontend Settings (Note: Only accessible if using a build tool like Vite)
# If using Vite, prefix with VITE_
VITE_API_URL=https://your-backend-api.vercel.app
